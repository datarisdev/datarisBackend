# Dependencias ligeras usadas por Vercel y desarrollo local básico
-r requirements.txt

# Dependencias pesadas SOLO para ejecución local/VM/Docker si usas procesamiento satelital completo.
# No las instales en Vercel porque superan el límite de 500 MB de Lambda.
celery>=5.4
redis>=5.0
numpy>=1.26
pandas>=2.2
rasterio>=1.3
geopandas>=0.14
planetary-computer>=1.0
pystac-client>=0.8
matplotlib>=3.8
Pillow>=10.0
